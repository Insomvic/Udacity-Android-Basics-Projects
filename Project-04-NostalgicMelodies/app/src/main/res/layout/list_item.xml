<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/song_row"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/song_image_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_margin="4dp"
        tools:src="@drawable/ic_zelda_oot" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/song_text_view"
            style="@style/ListTitleStyle"
            tools:text="Title Theme" />

        <TextView
            android:id="@+id/game_text_view"
            style="@style/ListGameStyle"
            tools:text="Ocarina of Time" />

    </LinearLayout>

</LinearLayout>